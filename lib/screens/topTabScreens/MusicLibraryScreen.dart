import 'package:flutter/material.dart';

class MusicLibraryScreen extends StatelessWidget {
  const MusicLibraryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Music Library Screen"),
      ),
    );
  }
}
